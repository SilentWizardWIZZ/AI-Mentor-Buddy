# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/ai_mentor_buddy

# Optional: If using separate database credentials
PGHOST=localhost
PGPORT=5432
PGDATABASE=ai_mentor_buddy
PGUSER=your_db_username
PGPASSWORD=your_db_password

# Application Configuration
NODE_ENV=development
PORT=5000